{
  "name": "taro-mystic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute taro-db --local --file=./schema.sql",
    "db:push": "wrangler d1 execute taro-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/node-server": "^1.8.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "wrangler": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
